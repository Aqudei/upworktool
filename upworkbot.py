from bot import api
import asyncio

from bot.utils import get_valid_access_token


async def main():
    # access_token = await get_valid_access_token()
    # jobs_data = await api.fetch_upwork_jobs_async()
    pass
    
asyncio.run(main())